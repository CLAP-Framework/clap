use_sim_time: true
carla:
  host: localhost
  port: 2000
  timeout: 1000
  synchronous_mode: false
  synchronous_mode_wait_for_vehicle_control_command: false
  fixed_delta_seconds: false
  ego_vehicle:

    role_name: ["hero", "ego_vehicle", "test_car"]
