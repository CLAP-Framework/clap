1、接受最后一个速度点进行测试（ok，目前10km/h  内为最后一个点速度）
2、减速度点增加死区测试。（not good，输出非线性）
3、选择900ms后速度点进行测试(ok,基于固定20cm间隔点计算)
4、使用速度点计算边界测试速度。(no)



1、边界处理逻辑
A、轨迹点获得边界，取最大最小（no）
B、比较边界。（no）
C、使用边界（no）

2、轨迹点插值（no）
3、横向加速度处理（no）
4、根据G3输出边界限制方向盘转角（ok）,50km/h限制
5、添加velocity_vector.size() = 0;时的策略，使用上一时刻的值。

1、icvcontrol  sample time 设置为0.15更改为0.02